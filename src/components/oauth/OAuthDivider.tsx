import '../../styles/auth-divider.css';

export const OAuthDivider = () => {
    return (
        <div className="auth-divider-title-container">
            <div className="auth-divider-title-mid-border">
                <h4 className="auth-divider-title">Or</h4>
            </div>
        </div>
    );
};
