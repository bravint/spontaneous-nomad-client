import { Header } from './Header';

export const Dashboard = () => {
    return (
        <>
            <Header />
            <section className="dashboard">
                <h1>Dashboard</h1>
            </section>
        </>
    );
};
